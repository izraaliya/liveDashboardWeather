---- For API Weather (Setting in Bash)
1. Intall Flask
   pip install flask flask-cors requests python-dotenv
3. Run
   python app.py

----For run Website
⚙️ 1. Requirements
Node.js (version 16 or later)
Check your version:
node -v
npm -v
If you don’t have Node.js yet, download it from:
👉 https://nodejs.org

📦 2. Installation

Go to your project directory:
Then install all dependencies:
npm install


This will install everything listed in your package.json.

▶️ 3. Run the App

To start the development server:

npm start



👉 http://localhost:3000
